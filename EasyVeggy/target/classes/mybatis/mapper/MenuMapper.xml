<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Menu">
	<select id="vLevel" parameterType="Map" resultType="String">
		select vlid from tvl
		<where>
			<foreach collection="array" item="item">
				and ${item} = 'Y'
			</foreach>
		</where>
	</select>
	
	<select id="recommend" parameterType="String" resultType="Map">
		select * from tmenu inner join trest using(restid) where vlid = #{vlevel}
	</select>
</mapper>